services:
  redis:
    image: redis:7-alpine
    container_name: eventy-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - eventy_redis_data:/data
    command: redis-server --appendonly yes

volumes:
  eventy_redis_data:
